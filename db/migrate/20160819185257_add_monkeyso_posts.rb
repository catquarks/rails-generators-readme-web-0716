class AddMonkeysoPosts < ActiveRecord::Migration
  def change
  	#this one doesn't have "to" in the file name so rails doesn't know it has anything to do with the posts table -- so it isnt' enough to just have "posts"
  end
end
